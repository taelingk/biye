# CardioFit — Windows WSL2 (NVIDIA GPU) 训练依赖
# 用于正式训练、ONNX 导出
# 安装: pip install -r requirements-wsl.txt
# 前置: WSL2 + NVIDIA GPU 驱动 + CUDA toolkit

# 深度学习框架 (GPU 版)
tensorflow[and-cuda]>=2.16
tf2onnx>=1.16
onnx>=1.16
onnxruntime-gpu>=1.18

# 数据处理
numpy>=1.24
pandas>=2.0
scipy>=1.10
h5py>=3.9
scikit-learn>=1.3

# 生理信号处理
neurokit2>=0.2.10

# 可视化
matplotlib>=3.7
seaborn>=0.12

# 配置管理
pyyaml>=6.0

# 进度条
tqdm>=4.65

# 测试
pytest>=7.4
