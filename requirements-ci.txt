# Dependencies needed by GitHub Actions lint/test jobs.
# Keep this focused so CI does not install heavyweight training-only packages.

tensorflow>=2.16,<2.22

numpy>=1.24
pandas>=2.0
scipy>=1.10
h5py>=3.9
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
joblib>=1.3

pytest>=7.4
